let table = "";

function tableOf(num) {
    for (let i=1; i<=10 ; i++) {
        table += num * i + " "
    }
    return table
}

// //Other display method
// let table = ""

// function tableOf(num) {
//     for (let i=1; i<=10 ; i++) {
//         table += num + "*" + i + "=" + num*i + " "
//         }
//     return table
// }

